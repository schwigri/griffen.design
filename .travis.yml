language: node_js

node_js:
  - "stable"
  - "lts/*"

branches:
  only:
    - master
    - staging
    - refactor

cache:
  directories:
    - node_modules

before_script:
  - "npm install"

script:
  - "npm run build"
